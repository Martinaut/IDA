<?xml version="1.0" encoding="UTF-8"?>
<solver>
    <!-- Domain model configuration -->
    <scanAnnotatedClasses>
        <packageInclude>at.jku.dke.ida.csp.domain</packageInclude>
    </scanAnnotatedClasses>

    <!-- Score configuration -->
    <scoreDirectorFactory>
        <scoreDrl>optaplanner/comp.drl</scoreDrl>
        <scoreDrl>optaplanner/general.drl</scoreDrl>
        <scoreDrl>optaplanner/noncomp.drl</scoreDrl>
    </scoreDirectorFactory>

    <!-- Optimization algorithms configuration -->
    <termination>
        <secondsSpentLimit>10</secondsSpentLimit>
    </termination>
    <constructionHeuristic>
        <constructionHeuristicType>ALLOCATE_ENTITY_FROM_QUEUE</constructionHeuristicType>
    </constructionHeuristic>
</solver>